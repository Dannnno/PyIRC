Basic program outline:

Class IRC_member:
    Contains all pertinent information (username, nick, etc)
    Also has a list/dict (figure this out) of all servers/channels
    Uses select to check if there is anything to read/write
    
GUI:
    Idk, kivy and make it look like every other irc client or something
    
Multiprocessing:
    Not sure where yet, but pretty sure this is going to have to be used somewhere
    Maybe threading instead - not sure how much of a performance difference (if any)
    there will be